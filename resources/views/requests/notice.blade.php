<x-guest-layout>
    <div class="mx-auto max-w-xl rounded-lg bg-white p-8 text-center shadow">
        <h1 class="text-2xl font-bold text-gray-900">{{ $title }}</h1>
        <p class="mt-4 text-gray-700">{{ $message }}</p>
    </div>
</x-guest-layout>
